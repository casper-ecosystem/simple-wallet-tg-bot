// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/predicate"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/recentinvoices"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/user"
)

// RecentInvoicesQuery is the builder for querying RecentInvoices entities.
type RecentInvoicesQuery struct {
	config
	ctx        *QueryContext
	order      []recentinvoices.OrderOption
	inters     []Interceptor
	predicates []predicate.RecentInvoices
	withOwner  *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RecentInvoicesQuery builder.
func (riq *RecentInvoicesQuery) Where(ps ...predicate.RecentInvoices) *RecentInvoicesQuery {
	riq.predicates = append(riq.predicates, ps...)
	return riq
}

// Limit the number of records to be returned by this query.
func (riq *RecentInvoicesQuery) Limit(limit int) *RecentInvoicesQuery {
	riq.ctx.Limit = &limit
	return riq
}

// Offset to start from.
func (riq *RecentInvoicesQuery) Offset(offset int) *RecentInvoicesQuery {
	riq.ctx.Offset = &offset
	return riq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (riq *RecentInvoicesQuery) Unique(unique bool) *RecentInvoicesQuery {
	riq.ctx.Unique = &unique
	return riq
}

// Order specifies how the records should be ordered.
func (riq *RecentInvoicesQuery) Order(o ...recentinvoices.OrderOption) *RecentInvoicesQuery {
	riq.order = append(riq.order, o...)
	return riq
}

// QueryOwner chains the current query on the "owner" edge.
func (riq *RecentInvoicesQuery) QueryOwner() *UserQuery {
	query := (&UserClient{config: riq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := riq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := riq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(recentinvoices.Table, recentinvoices.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, recentinvoices.OwnerTable, recentinvoices.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(riq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RecentInvoices entity from the query.
// Returns a *NotFoundError when no RecentInvoices was found.
func (riq *RecentInvoicesQuery) First(ctx context.Context) (*RecentInvoices, error) {
	nodes, err := riq.Limit(1).All(setContextOp(ctx, riq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{recentinvoices.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (riq *RecentInvoicesQuery) FirstX(ctx context.Context) *RecentInvoices {
	node, err := riq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RecentInvoices ID from the query.
// Returns a *NotFoundError when no RecentInvoices ID was found.
func (riq *RecentInvoicesQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = riq.Limit(1).IDs(setContextOp(ctx, riq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{recentinvoices.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (riq *RecentInvoicesQuery) FirstIDX(ctx context.Context) int {
	id, err := riq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RecentInvoices entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RecentInvoices entity is found.
// Returns a *NotFoundError when no RecentInvoices entities are found.
func (riq *RecentInvoicesQuery) Only(ctx context.Context) (*RecentInvoices, error) {
	nodes, err := riq.Limit(2).All(setContextOp(ctx, riq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{recentinvoices.Label}
	default:
		return nil, &NotSingularError{recentinvoices.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (riq *RecentInvoicesQuery) OnlyX(ctx context.Context) *RecentInvoices {
	node, err := riq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RecentInvoices ID in the query.
// Returns a *NotSingularError when more than one RecentInvoices ID is found.
// Returns a *NotFoundError when no entities are found.
func (riq *RecentInvoicesQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = riq.Limit(2).IDs(setContextOp(ctx, riq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{recentinvoices.Label}
	default:
		err = &NotSingularError{recentinvoices.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (riq *RecentInvoicesQuery) OnlyIDX(ctx context.Context) int {
	id, err := riq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RecentInvoicesSlice.
func (riq *RecentInvoicesQuery) All(ctx context.Context) ([]*RecentInvoices, error) {
	ctx = setContextOp(ctx, riq.ctx, "All")
	if err := riq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RecentInvoices, *RecentInvoicesQuery]()
	return withInterceptors[[]*RecentInvoices](ctx, riq, qr, riq.inters)
}

// AllX is like All, but panics if an error occurs.
func (riq *RecentInvoicesQuery) AllX(ctx context.Context) []*RecentInvoices {
	nodes, err := riq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RecentInvoices IDs.
func (riq *RecentInvoicesQuery) IDs(ctx context.Context) (ids []int, err error) {
	if riq.ctx.Unique == nil && riq.path != nil {
		riq.Unique(true)
	}
	ctx = setContextOp(ctx, riq.ctx, "IDs")
	if err = riq.Select(recentinvoices.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (riq *RecentInvoicesQuery) IDsX(ctx context.Context) []int {
	ids, err := riq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (riq *RecentInvoicesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, riq.ctx, "Count")
	if err := riq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, riq, querierCount[*RecentInvoicesQuery](), riq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (riq *RecentInvoicesQuery) CountX(ctx context.Context) int {
	count, err := riq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (riq *RecentInvoicesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, riq.ctx, "Exist")
	switch _, err := riq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (riq *RecentInvoicesQuery) ExistX(ctx context.Context) bool {
	exist, err := riq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RecentInvoicesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (riq *RecentInvoicesQuery) Clone() *RecentInvoicesQuery {
	if riq == nil {
		return nil
	}
	return &RecentInvoicesQuery{
		config:     riq.config,
		ctx:        riq.ctx.Clone(),
		order:      append([]recentinvoices.OrderOption{}, riq.order...),
		inters:     append([]Interceptor{}, riq.inters...),
		predicates: append([]predicate.RecentInvoices{}, riq.predicates...),
		withOwner:  riq.withOwner.Clone(),
		// clone intermediate query.
		sql:  riq.sql.Clone(),
		path: riq.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (riq *RecentInvoicesQuery) WithOwner(opts ...func(*UserQuery)) *RecentInvoicesQuery {
	query := (&UserClient{config: riq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	riq.withOwner = query
	return riq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RecentInvoices.Query().
//		GroupBy(recentinvoices.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (riq *RecentInvoicesQuery) GroupBy(field string, fields ...string) *RecentInvoicesGroupBy {
	riq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RecentInvoicesGroupBy{build: riq}
	grbuild.flds = &riq.ctx.Fields
	grbuild.label = recentinvoices.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//	}
//
//	client.RecentInvoices.Query().
//		Select(recentinvoices.FieldStatus).
//		Scan(ctx, &v)
func (riq *RecentInvoicesQuery) Select(fields ...string) *RecentInvoicesSelect {
	riq.ctx.Fields = append(riq.ctx.Fields, fields...)
	sbuild := &RecentInvoicesSelect{RecentInvoicesQuery: riq}
	sbuild.label = recentinvoices.Label
	sbuild.flds, sbuild.scan = &riq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RecentInvoicesSelect configured with the given aggregations.
func (riq *RecentInvoicesQuery) Aggregate(fns ...AggregateFunc) *RecentInvoicesSelect {
	return riq.Select().Aggregate(fns...)
}

func (riq *RecentInvoicesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range riq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, riq); err != nil {
				return err
			}
		}
	}
	for _, f := range riq.ctx.Fields {
		if !recentinvoices.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if riq.path != nil {
		prev, err := riq.path(ctx)
		if err != nil {
			return err
		}
		riq.sql = prev
	}
	return nil
}

func (riq *RecentInvoicesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RecentInvoices, error) {
	var (
		nodes       = []*RecentInvoices{}
		withFKs     = riq.withFKs
		_spec       = riq.querySpec()
		loadedTypes = [1]bool{
			riq.withOwner != nil,
		}
	)
	if riq.withOwner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, recentinvoices.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RecentInvoices).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RecentInvoices{config: riq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, riq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := riq.withOwner; query != nil {
		if err := riq.loadOwner(ctx, query, nodes, nil,
			func(n *RecentInvoices, e *User) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (riq *RecentInvoicesQuery) loadOwner(ctx context.Context, query *UserQuery, nodes []*RecentInvoices, init func(*RecentInvoices), assign func(*RecentInvoices, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*RecentInvoices)
	for i := range nodes {
		if nodes[i].user_recent_invoices == nil {
			continue
		}
		fk := *nodes[i].user_recent_invoices
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_recent_invoices" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (riq *RecentInvoicesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := riq.querySpec()
	_spec.Node.Columns = riq.ctx.Fields
	if len(riq.ctx.Fields) > 0 {
		_spec.Unique = riq.ctx.Unique != nil && *riq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, riq.driver, _spec)
}

func (riq *RecentInvoicesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(recentinvoices.Table, recentinvoices.Columns, sqlgraph.NewFieldSpec(recentinvoices.FieldID, field.TypeInt))
	_spec.From = riq.sql
	if unique := riq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if riq.path != nil {
		_spec.Unique = true
	}
	if fields := riq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, recentinvoices.FieldID)
		for i := range fields {
			if fields[i] != recentinvoices.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := riq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := riq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := riq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := riq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (riq *RecentInvoicesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(riq.driver.Dialect())
	t1 := builder.Table(recentinvoices.Table)
	columns := riq.ctx.Fields
	if len(columns) == 0 {
		columns = recentinvoices.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if riq.sql != nil {
		selector = riq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if riq.ctx.Unique != nil && *riq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range riq.predicates {
		p(selector)
	}
	for _, p := range riq.order {
		p(selector)
	}
	if offset := riq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := riq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RecentInvoicesGroupBy is the group-by builder for RecentInvoices entities.
type RecentInvoicesGroupBy struct {
	selector
	build *RecentInvoicesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rigb *RecentInvoicesGroupBy) Aggregate(fns ...AggregateFunc) *RecentInvoicesGroupBy {
	rigb.fns = append(rigb.fns, fns...)
	return rigb
}

// Scan applies the selector query and scans the result into the given value.
func (rigb *RecentInvoicesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rigb.build.ctx, "GroupBy")
	if err := rigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecentInvoicesQuery, *RecentInvoicesGroupBy](ctx, rigb.build, rigb, rigb.build.inters, v)
}

func (rigb *RecentInvoicesGroupBy) sqlScan(ctx context.Context, root *RecentInvoicesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rigb.fns))
	for _, fn := range rigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rigb.flds)+len(rigb.fns))
		for _, f := range *rigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RecentInvoicesSelect is the builder for selecting fields of RecentInvoices entities.
type RecentInvoicesSelect struct {
	*RecentInvoicesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ris *RecentInvoicesSelect) Aggregate(fns ...AggregateFunc) *RecentInvoicesSelect {
	ris.fns = append(ris.fns, fns...)
	return ris
}

// Scan applies the selector query and scans the result into the given value.
func (ris *RecentInvoicesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ris.ctx, "Select")
	if err := ris.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecentInvoicesQuery, *RecentInvoicesSelect](ctx, ris.RecentInvoicesQuery, ris, ris.inters, v)
}

func (ris *RecentInvoicesSelect) sqlScan(ctx context.Context, root *RecentInvoicesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ris.fns))
	for _, fn := range ris.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ris.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ris.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
