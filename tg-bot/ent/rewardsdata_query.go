// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Simplewallethq/tg-bot/ent/predicate"
	"github.com/Simplewallethq/tg-bot/ent/rewardsdata"
	"github.com/Simplewallethq/tg-bot/ent/user"
)

// RewardsDataQuery is the builder for querying RewardsData entities.
type RewardsDataQuery struct {
	config
	ctx        *QueryContext
	order      []rewardsdata.OrderOption
	inters     []Interceptor
	predicates []predicate.RewardsData
	withOwner  *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RewardsDataQuery builder.
func (rdq *RewardsDataQuery) Where(ps ...predicate.RewardsData) *RewardsDataQuery {
	rdq.predicates = append(rdq.predicates, ps...)
	return rdq
}

// Limit the number of records to be returned by this query.
func (rdq *RewardsDataQuery) Limit(limit int) *RewardsDataQuery {
	rdq.ctx.Limit = &limit
	return rdq
}

// Offset to start from.
func (rdq *RewardsDataQuery) Offset(offset int) *RewardsDataQuery {
	rdq.ctx.Offset = &offset
	return rdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rdq *RewardsDataQuery) Unique(unique bool) *RewardsDataQuery {
	rdq.ctx.Unique = &unique
	return rdq
}

// Order specifies how the records should be ordered.
func (rdq *RewardsDataQuery) Order(o ...rewardsdata.OrderOption) *RewardsDataQuery {
	rdq.order = append(rdq.order, o...)
	return rdq
}

// QueryOwner chains the current query on the "owner" edge.
func (rdq *RewardsDataQuery) QueryOwner() *UserQuery {
	query := (&UserClient{config: rdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rewardsdata.Table, rewardsdata.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, rewardsdata.OwnerTable, rewardsdata.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(rdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RewardsData entity from the query.
// Returns a *NotFoundError when no RewardsData was found.
func (rdq *RewardsDataQuery) First(ctx context.Context) (*RewardsData, error) {
	nodes, err := rdq.Limit(1).All(setContextOp(ctx, rdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rewardsdata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rdq *RewardsDataQuery) FirstX(ctx context.Context) *RewardsData {
	node, err := rdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RewardsData ID from the query.
// Returns a *NotFoundError when no RewardsData ID was found.
func (rdq *RewardsDataQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rdq.Limit(1).IDs(setContextOp(ctx, rdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rewardsdata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rdq *RewardsDataQuery) FirstIDX(ctx context.Context) int {
	id, err := rdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RewardsData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RewardsData entity is found.
// Returns a *NotFoundError when no RewardsData entities are found.
func (rdq *RewardsDataQuery) Only(ctx context.Context) (*RewardsData, error) {
	nodes, err := rdq.Limit(2).All(setContextOp(ctx, rdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rewardsdata.Label}
	default:
		return nil, &NotSingularError{rewardsdata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rdq *RewardsDataQuery) OnlyX(ctx context.Context) *RewardsData {
	node, err := rdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RewardsData ID in the query.
// Returns a *NotSingularError when more than one RewardsData ID is found.
// Returns a *NotFoundError when no entities are found.
func (rdq *RewardsDataQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rdq.Limit(2).IDs(setContextOp(ctx, rdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rewardsdata.Label}
	default:
		err = &NotSingularError{rewardsdata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rdq *RewardsDataQuery) OnlyIDX(ctx context.Context) int {
	id, err := rdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RewardsDataSlice.
func (rdq *RewardsDataQuery) All(ctx context.Context) ([]*RewardsData, error) {
	ctx = setContextOp(ctx, rdq.ctx, "All")
	if err := rdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RewardsData, *RewardsDataQuery]()
	return withInterceptors[[]*RewardsData](ctx, rdq, qr, rdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rdq *RewardsDataQuery) AllX(ctx context.Context) []*RewardsData {
	nodes, err := rdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RewardsData IDs.
func (rdq *RewardsDataQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rdq.ctx.Unique == nil && rdq.path != nil {
		rdq.Unique(true)
	}
	ctx = setContextOp(ctx, rdq.ctx, "IDs")
	if err = rdq.Select(rewardsdata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rdq *RewardsDataQuery) IDsX(ctx context.Context) []int {
	ids, err := rdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rdq *RewardsDataQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rdq.ctx, "Count")
	if err := rdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rdq, querierCount[*RewardsDataQuery](), rdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rdq *RewardsDataQuery) CountX(ctx context.Context) int {
	count, err := rdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rdq *RewardsDataQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rdq.ctx, "Exist")
	switch _, err := rdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rdq *RewardsDataQuery) ExistX(ctx context.Context) bool {
	exist, err := rdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RewardsDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rdq *RewardsDataQuery) Clone() *RewardsDataQuery {
	if rdq == nil {
		return nil
	}
	return &RewardsDataQuery{
		config:     rdq.config,
		ctx:        rdq.ctx.Clone(),
		order:      append([]rewardsdata.OrderOption{}, rdq.order...),
		inters:     append([]Interceptor{}, rdq.inters...),
		predicates: append([]predicate.RewardsData{}, rdq.predicates...),
		withOwner:  rdq.withOwner.Clone(),
		// clone intermediate query.
		sql:  rdq.sql.Clone(),
		path: rdq.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (rdq *RewardsDataQuery) WithOwner(opts ...func(*UserQuery)) *RewardsDataQuery {
	query := (&UserClient{config: rdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rdq.withOwner = query
	return rdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Validator string `json:"validator,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RewardsData.Query().
//		GroupBy(rewardsdata.FieldValidator).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rdq *RewardsDataQuery) GroupBy(field string, fields ...string) *RewardsDataGroupBy {
	rdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RewardsDataGroupBy{build: rdq}
	grbuild.flds = &rdq.ctx.Fields
	grbuild.label = rewardsdata.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Validator string `json:"validator,omitempty"`
//	}
//
//	client.RewardsData.Query().
//		Select(rewardsdata.FieldValidator).
//		Scan(ctx, &v)
func (rdq *RewardsDataQuery) Select(fields ...string) *RewardsDataSelect {
	rdq.ctx.Fields = append(rdq.ctx.Fields, fields...)
	sbuild := &RewardsDataSelect{RewardsDataQuery: rdq}
	sbuild.label = rewardsdata.Label
	sbuild.flds, sbuild.scan = &rdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RewardsDataSelect configured with the given aggregations.
func (rdq *RewardsDataQuery) Aggregate(fns ...AggregateFunc) *RewardsDataSelect {
	return rdq.Select().Aggregate(fns...)
}

func (rdq *RewardsDataQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rdq); err != nil {
				return err
			}
		}
	}
	for _, f := range rdq.ctx.Fields {
		if !rewardsdata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rdq.path != nil {
		prev, err := rdq.path(ctx)
		if err != nil {
			return err
		}
		rdq.sql = prev
	}
	return nil
}

func (rdq *RewardsDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RewardsData, error) {
	var (
		nodes       = []*RewardsData{}
		withFKs     = rdq.withFKs
		_spec       = rdq.querySpec()
		loadedTypes = [1]bool{
			rdq.withOwner != nil,
		}
	)
	if rdq.withOwner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, rewardsdata.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RewardsData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RewardsData{config: rdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rdq.withOwner; query != nil {
		if err := rdq.loadOwner(ctx, query, nodes, nil,
			func(n *RewardsData, e *User) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rdq *RewardsDataQuery) loadOwner(ctx context.Context, query *UserQuery, nodes []*RewardsData, init func(*RewardsData), assign func(*RewardsData, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*RewardsData)
	for i := range nodes {
		if nodes[i].user_rewards_data == nil {
			continue
		}
		fk := *nodes[i].user_rewards_data
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_rewards_data" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rdq *RewardsDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rdq.querySpec()
	_spec.Node.Columns = rdq.ctx.Fields
	if len(rdq.ctx.Fields) > 0 {
		_spec.Unique = rdq.ctx.Unique != nil && *rdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rdq.driver, _spec)
}

func (rdq *RewardsDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rewardsdata.Table, rewardsdata.Columns, sqlgraph.NewFieldSpec(rewardsdata.FieldID, field.TypeInt))
	_spec.From = rdq.sql
	if unique := rdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rdq.path != nil {
		_spec.Unique = true
	}
	if fields := rdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rewardsdata.FieldID)
		for i := range fields {
			if fields[i] != rewardsdata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rdq *RewardsDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rdq.driver.Dialect())
	t1 := builder.Table(rewardsdata.Table)
	columns := rdq.ctx.Fields
	if len(columns) == 0 {
		columns = rewardsdata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rdq.sql != nil {
		selector = rdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rdq.ctx.Unique != nil && *rdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rdq.predicates {
		p(selector)
	}
	for _, p := range rdq.order {
		p(selector)
	}
	if offset := rdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RewardsDataGroupBy is the group-by builder for RewardsData entities.
type RewardsDataGroupBy struct {
	selector
	build *RewardsDataQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rdgb *RewardsDataGroupBy) Aggregate(fns ...AggregateFunc) *RewardsDataGroupBy {
	rdgb.fns = append(rdgb.fns, fns...)
	return rdgb
}

// Scan applies the selector query and scans the result into the given value.
func (rdgb *RewardsDataGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rdgb.build.ctx, "GroupBy")
	if err := rdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RewardsDataQuery, *RewardsDataGroupBy](ctx, rdgb.build, rdgb, rdgb.build.inters, v)
}

func (rdgb *RewardsDataGroupBy) sqlScan(ctx context.Context, root *RewardsDataQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rdgb.fns))
	for _, fn := range rdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rdgb.flds)+len(rdgb.fns))
		for _, f := range *rdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RewardsDataSelect is the builder for selecting fields of RewardsData entities.
type RewardsDataSelect struct {
	*RewardsDataQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rds *RewardsDataSelect) Aggregate(fns ...AggregateFunc) *RewardsDataSelect {
	rds.fns = append(rds.fns, fns...)
	return rds
}

// Scan applies the selector query and scans the result into the given value.
func (rds *RewardsDataSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rds.ctx, "Select")
	if err := rds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RewardsDataQuery, *RewardsDataSelect](ctx, rds.RewardsDataQuery, rds, rds.inters, v)
}

func (rds *RewardsDataSelect) sqlScan(ctx context.Context, root *RewardsDataQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rds.fns))
	for _, fn := range rds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
