-- Create "private_keys" table
CREATE TABLE "private_keys" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "privat_key" bytea NULL, "user_private_key" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "private_keys_users_PrivateKey" FOREIGN KEY ("user_private_key") REFERENCES "users" ("id") ON UPDATE NO ACTION ON DELETE CASCADE);
-- Create index "private_keys_user_private_key_key" to table: "private_keys"
CREATE UNIQUE INDEX "private_keys_user_private_key_key" ON "private_keys" ("user_private_key");
-- Drop "privat_keys" table
DROP TABLE "privat_keys";
