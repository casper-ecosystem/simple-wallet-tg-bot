// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/adressbook"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/predicate"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/user"
)

// AdressBookQuery is the builder for querying AdressBook entities.
type AdressBookQuery struct {
	config
	ctx        *QueryContext
	order      []adressbook.OrderOption
	inters     []Interceptor
	predicates []predicate.AdressBook
	withOwner  *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AdressBookQuery builder.
func (abq *AdressBookQuery) Where(ps ...predicate.AdressBook) *AdressBookQuery {
	abq.predicates = append(abq.predicates, ps...)
	return abq
}

// Limit the number of records to be returned by this query.
func (abq *AdressBookQuery) Limit(limit int) *AdressBookQuery {
	abq.ctx.Limit = &limit
	return abq
}

// Offset to start from.
func (abq *AdressBookQuery) Offset(offset int) *AdressBookQuery {
	abq.ctx.Offset = &offset
	return abq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (abq *AdressBookQuery) Unique(unique bool) *AdressBookQuery {
	abq.ctx.Unique = &unique
	return abq
}

// Order specifies how the records should be ordered.
func (abq *AdressBookQuery) Order(o ...adressbook.OrderOption) *AdressBookQuery {
	abq.order = append(abq.order, o...)
	return abq
}

// QueryOwner chains the current query on the "owner" edge.
func (abq *AdressBookQuery) QueryOwner() *UserQuery {
	query := (&UserClient{config: abq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := abq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := abq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(adressbook.Table, adressbook.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, adressbook.OwnerTable, adressbook.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(abq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AdressBook entity from the query.
// Returns a *NotFoundError when no AdressBook was found.
func (abq *AdressBookQuery) First(ctx context.Context) (*AdressBook, error) {
	nodes, err := abq.Limit(1).All(setContextOp(ctx, abq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{adressbook.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (abq *AdressBookQuery) FirstX(ctx context.Context) *AdressBook {
	node, err := abq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AdressBook ID from the query.
// Returns a *NotFoundError when no AdressBook ID was found.
func (abq *AdressBookQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = abq.Limit(1).IDs(setContextOp(ctx, abq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{adressbook.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (abq *AdressBookQuery) FirstIDX(ctx context.Context) int {
	id, err := abq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AdressBook entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AdressBook entity is found.
// Returns a *NotFoundError when no AdressBook entities are found.
func (abq *AdressBookQuery) Only(ctx context.Context) (*AdressBook, error) {
	nodes, err := abq.Limit(2).All(setContextOp(ctx, abq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{adressbook.Label}
	default:
		return nil, &NotSingularError{adressbook.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (abq *AdressBookQuery) OnlyX(ctx context.Context) *AdressBook {
	node, err := abq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AdressBook ID in the query.
// Returns a *NotSingularError when more than one AdressBook ID is found.
// Returns a *NotFoundError when no entities are found.
func (abq *AdressBookQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = abq.Limit(2).IDs(setContextOp(ctx, abq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{adressbook.Label}
	default:
		err = &NotSingularError{adressbook.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (abq *AdressBookQuery) OnlyIDX(ctx context.Context) int {
	id, err := abq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AdressBooks.
func (abq *AdressBookQuery) All(ctx context.Context) ([]*AdressBook, error) {
	ctx = setContextOp(ctx, abq.ctx, "All")
	if err := abq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AdressBook, *AdressBookQuery]()
	return withInterceptors[[]*AdressBook](ctx, abq, qr, abq.inters)
}

// AllX is like All, but panics if an error occurs.
func (abq *AdressBookQuery) AllX(ctx context.Context) []*AdressBook {
	nodes, err := abq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AdressBook IDs.
func (abq *AdressBookQuery) IDs(ctx context.Context) (ids []int, err error) {
	if abq.ctx.Unique == nil && abq.path != nil {
		abq.Unique(true)
	}
	ctx = setContextOp(ctx, abq.ctx, "IDs")
	if err = abq.Select(adressbook.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (abq *AdressBookQuery) IDsX(ctx context.Context) []int {
	ids, err := abq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (abq *AdressBookQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, abq.ctx, "Count")
	if err := abq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, abq, querierCount[*AdressBookQuery](), abq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (abq *AdressBookQuery) CountX(ctx context.Context) int {
	count, err := abq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (abq *AdressBookQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, abq.ctx, "Exist")
	switch _, err := abq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (abq *AdressBookQuery) ExistX(ctx context.Context) bool {
	exist, err := abq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AdressBookQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (abq *AdressBookQuery) Clone() *AdressBookQuery {
	if abq == nil {
		return nil
	}
	return &AdressBookQuery{
		config:     abq.config,
		ctx:        abq.ctx.Clone(),
		order:      append([]adressbook.OrderOption{}, abq.order...),
		inters:     append([]Interceptor{}, abq.inters...),
		predicates: append([]predicate.AdressBook{}, abq.predicates...),
		withOwner:  abq.withOwner.Clone(),
		// clone intermediate query.
		sql:  abq.sql.Clone(),
		path: abq.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (abq *AdressBookQuery) WithOwner(opts ...func(*UserQuery)) *AdressBookQuery {
	query := (&UserClient{config: abq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	abq.withOwner = query
	return abq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Address string `json:"address,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AdressBook.Query().
//		GroupBy(adressbook.FieldAddress).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (abq *AdressBookQuery) GroupBy(field string, fields ...string) *AdressBookGroupBy {
	abq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AdressBookGroupBy{build: abq}
	grbuild.flds = &abq.ctx.Fields
	grbuild.label = adressbook.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Address string `json:"address,omitempty"`
//	}
//
//	client.AdressBook.Query().
//		Select(adressbook.FieldAddress).
//		Scan(ctx, &v)
func (abq *AdressBookQuery) Select(fields ...string) *AdressBookSelect {
	abq.ctx.Fields = append(abq.ctx.Fields, fields...)
	sbuild := &AdressBookSelect{AdressBookQuery: abq}
	sbuild.label = adressbook.Label
	sbuild.flds, sbuild.scan = &abq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AdressBookSelect configured with the given aggregations.
func (abq *AdressBookQuery) Aggregate(fns ...AggregateFunc) *AdressBookSelect {
	return abq.Select().Aggregate(fns...)
}

func (abq *AdressBookQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range abq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, abq); err != nil {
				return err
			}
		}
	}
	for _, f := range abq.ctx.Fields {
		if !adressbook.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if abq.path != nil {
		prev, err := abq.path(ctx)
		if err != nil {
			return err
		}
		abq.sql = prev
	}
	return nil
}

func (abq *AdressBookQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AdressBook, error) {
	var (
		nodes       = []*AdressBook{}
		withFKs     = abq.withFKs
		_spec       = abq.querySpec()
		loadedTypes = [1]bool{
			abq.withOwner != nil,
		}
	)
	if abq.withOwner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, adressbook.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AdressBook).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AdressBook{config: abq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, abq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := abq.withOwner; query != nil {
		if err := abq.loadOwner(ctx, query, nodes, nil,
			func(n *AdressBook, e *User) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (abq *AdressBookQuery) loadOwner(ctx context.Context, query *UserQuery, nodes []*AdressBook, init func(*AdressBook), assign func(*AdressBook, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*AdressBook)
	for i := range nodes {
		if nodes[i].user_address_book == nil {
			continue
		}
		fk := *nodes[i].user_address_book
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_address_book" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (abq *AdressBookQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := abq.querySpec()
	_spec.Node.Columns = abq.ctx.Fields
	if len(abq.ctx.Fields) > 0 {
		_spec.Unique = abq.ctx.Unique != nil && *abq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, abq.driver, _spec)
}

func (abq *AdressBookQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(adressbook.Table, adressbook.Columns, sqlgraph.NewFieldSpec(adressbook.FieldID, field.TypeInt))
	_spec.From = abq.sql
	if unique := abq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if abq.path != nil {
		_spec.Unique = true
	}
	if fields := abq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, adressbook.FieldID)
		for i := range fields {
			if fields[i] != adressbook.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := abq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := abq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := abq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := abq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (abq *AdressBookQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(abq.driver.Dialect())
	t1 := builder.Table(adressbook.Table)
	columns := abq.ctx.Fields
	if len(columns) == 0 {
		columns = adressbook.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if abq.sql != nil {
		selector = abq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if abq.ctx.Unique != nil && *abq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range abq.predicates {
		p(selector)
	}
	for _, p := range abq.order {
		p(selector)
	}
	if offset := abq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := abq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AdressBookGroupBy is the group-by builder for AdressBook entities.
type AdressBookGroupBy struct {
	selector
	build *AdressBookQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (abgb *AdressBookGroupBy) Aggregate(fns ...AggregateFunc) *AdressBookGroupBy {
	abgb.fns = append(abgb.fns, fns...)
	return abgb
}

// Scan applies the selector query and scans the result into the given value.
func (abgb *AdressBookGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, abgb.build.ctx, "GroupBy")
	if err := abgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AdressBookQuery, *AdressBookGroupBy](ctx, abgb.build, abgb, abgb.build.inters, v)
}

func (abgb *AdressBookGroupBy) sqlScan(ctx context.Context, root *AdressBookQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(abgb.fns))
	for _, fn := range abgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*abgb.flds)+len(abgb.fns))
		for _, f := range *abgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*abgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := abgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AdressBookSelect is the builder for selecting fields of AdressBook entities.
type AdressBookSelect struct {
	*AdressBookQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (abs *AdressBookSelect) Aggregate(fns ...AggregateFunc) *AdressBookSelect {
	abs.fns = append(abs.fns, fns...)
	return abs
}

// Scan applies the selector query and scans the result into the given value.
func (abs *AdressBookSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, abs.ctx, "Select")
	if err := abs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AdressBookQuery, *AdressBookSelect](ctx, abs.AdressBookQuery, abs, abs.inters, v)
}

func (abs *AdressBookSelect) sqlScan(ctx context.Context, root *AdressBookQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(abs.fns))
	for _, fn := range abs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*abs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := abs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
