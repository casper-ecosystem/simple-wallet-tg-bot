// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/privatekeys"
	"github.com/Simplewallethq/simple-wallet-tg-bot/tg-bot/ent/user"
)

// PrivateKeys is the model entity for the PrivateKeys schema.
type PrivateKeys struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// PrivateKey holds the value of the "private_key" field.
	PrivateKey []byte `json:"private_key,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PrivateKeysQuery when eager-loading is set.
	Edges            PrivateKeysEdges `json:"edges"`
	user_private_key *int64
	selectValues     sql.SelectValues
}

// PrivateKeysEdges holds the relations/edges for other nodes in the graph.
type PrivateKeysEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PrivateKeysEdges) OwnerOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.Owner == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Owner, nil
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PrivateKeys) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case privatekeys.FieldPrivateKey:
			values[i] = new([]byte)
		case privatekeys.FieldID:
			values[i] = new(sql.NullInt64)
		case privatekeys.ForeignKeys[0]: // user_private_key
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PrivateKeys fields.
func (pk *PrivateKeys) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case privatekeys.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pk.ID = int(value.Int64)
		case privatekeys.FieldPrivateKey:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field private_key", values[i])
			} else if value != nil {
				pk.PrivateKey = *value
			}
		case privatekeys.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_private_key", value)
			} else if value.Valid {
				pk.user_private_key = new(int64)
				*pk.user_private_key = int64(value.Int64)
			}
		default:
			pk.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PrivateKeys.
// This includes values selected through modifiers, order, etc.
func (pk *PrivateKeys) Value(name string) (ent.Value, error) {
	return pk.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the PrivateKeys entity.
func (pk *PrivateKeys) QueryOwner() *UserQuery {
	return NewPrivateKeysClient(pk.config).QueryOwner(pk)
}

// Update returns a builder for updating this PrivateKeys.
// Note that you need to call PrivateKeys.Unwrap() before calling this method if this PrivateKeys
// was returned from a transaction, and the transaction was committed or rolled back.
func (pk *PrivateKeys) Update() *PrivateKeysUpdateOne {
	return NewPrivateKeysClient(pk.config).UpdateOne(pk)
}

// Unwrap unwraps the PrivateKeys entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pk *PrivateKeys) Unwrap() *PrivateKeys {
	_tx, ok := pk.config.driver.(*txDriver)
	if !ok {
		panic("ent: PrivateKeys is not a transactional entity")
	}
	pk.config.driver = _tx.drv
	return pk
}

// String implements the fmt.Stringer.
func (pk *PrivateKeys) String() string {
	var builder strings.Builder
	builder.WriteString("PrivateKeys(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pk.ID))
	builder.WriteString("private_key=")
	builder.WriteString(fmt.Sprintf("%v", pk.PrivateKey))
	builder.WriteByte(')')
	return builder.String()
}

// PrivateKeysSlice is a parsable slice of PrivateKeys.
type PrivateKeysSlice []*PrivateKeys
